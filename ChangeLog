2017.06.13, v1.3.1

fix:
- (package) rm:docker

feature:
- (api) createFileWhenNotExist: add dd fallback: mac os
- (loop) add $FILE, $SIZE


2017.06.02, v1.3.0

fix:
- refactor(loop) rm prefix "__"
- (travis) shellcheck

feature:
- (package) scripts: prewisdom
- (loop) add ability to checkDependencies
- (loop) lib -> bin
- (package) last version of shellcheck from docker
- (package) wisdom


2017.06.01, v1.2.0

fix:
- (dockerignore) rm package.json
- (loop) $DIR -> $SIZE

feature:
- (loop) add ability to pass arguments via environment variables
- (package) scripts: postwisdom:done


2017.05.31, v1.1.0

feature:
- (Dockerfile) add WORKDIR /root


2017.05.31, v1.0.2

fix:
- (package) wisdom -> wisdom:done

feature:
- (package) add repository


2017.05.31, v1.0.1

fix:
- (package) scripts: wisdom
- (loop) mount -> loop
- feature(package) build, wisdom -> add prefix docker:

feature:
- (package) add repository
- (package) build, wisdom -> add prefix docker:

