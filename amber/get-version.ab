pub fun get_version() {
    let BASEDIR = trust $dirname "\$0"$
    let PACKAGE="{BASEDIR}/../package.json";
    let VERSION= trust $grep '"version":' "{PACKAGE}"| cut -d\\" -f4$

    return VERSION;
}
